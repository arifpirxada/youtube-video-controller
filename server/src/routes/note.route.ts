import { Router } from 'express';

const router = Router();

router.post('/notes/:videoId', (req, res) => {

})

router.patch('/notes/:noteId', (req, res) => {

})

router.delete('/notes/:noteId', (req, res) => {
    
})

export default router;
